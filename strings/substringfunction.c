#include<stdio.h>
#include<string.h>

int sub_string(char str[])
{
	int pos,length;
	char substr[1000];
	
	printf("Enter the position : ");
	gets(pos);
	
	printf("Enter the length : ");
	gets(length);
	
	while(str[1000]<length)
	{
		 
	
	}


}

void main()
{
	char str[50]; 
	int substring;
	
	printf("Enter the element : ");
	gets(ch1);
	
	substring=sub_string(str);
	
	printf("substring = %d", substring);
}