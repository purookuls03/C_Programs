#include<stdio.h>
//#include<string.h>

void main()
{
	char ch[50];
	char ch2[50];
	
	printf("enter the first elemnet : ");
	scanf("%s", &ch);
	
	printf("enter the first elemnet : ", ch);
	printf("%s", ch);
	
	/*if(strcmpi(ch,ch2)==0)
	{
		printf("both are equal");
	}
	
	else
		printf("both are not equal");*/
}