#include<stdio.h>

void main()
{
    int size,sum=0,count;
    int avg, max=0;
    
    printf("Enter the size of array : ");
    scanf("%d", &size);
    int arr[size];
    
    printf("Enter the elements of array : \n");

    for(count=0;count<size;count++)
    {
        scanf("%d", &arr[count]);
    }

    for(count=0;count<size;count++)
    {
        if (arr[count]>max)
     printf("maximum is %d", arr[i]);
     
     else 
      printf("max is maximum");

    
    }

        
}