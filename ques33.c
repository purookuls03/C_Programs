#include<stdio.h>

void main()
{
    int match_sticks = 21, uc, cc;
    
	while(match_sticks>=1)
    {
        printf("Total Match Sticks: %d\n", match_sticks);
        
		printf("Pick up the match sticks between (1 to 4): ");
        scanf("%d", &uc);

    }
}
