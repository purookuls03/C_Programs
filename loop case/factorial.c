//WAp to calculate of a factrial of a number

#include<stdio.h>

void main()
{
	int num,i,fact=1;
	

	printf("Input the number : ");
	scanf("%d", &num);

	for (i=1;a<=num;i++)
	{
		fact=fact*a;
	}
 	 printf("factorial = %d", fact);
}